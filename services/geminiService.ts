// This service is currently unused as AI features are disabled for this version.
// Kept as a placeholder for future AI integrations.

export const analyzePdf = async () => {
  throw new Error("AI features are not enabled in this build.");
};

export const chatWithPdfContext = async () => {
  throw new Error("AI features are not enabled in this build.");
};